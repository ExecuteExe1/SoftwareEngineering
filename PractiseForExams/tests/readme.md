   " To properly run said file:
       run test:coverage
       and npm test"

 "Just make sure you have installed the supertest thing,my package.json has it"
" #Commands to install the test"
 "npm install --save-dev jest supertest"

 "For ava ,you gotta install it first"
 "Install command: npm install --save-dev ava"
 "Basic run:npm test
# or
npx ava"

"For specific Test:npx ava path/to/your/test-file.js"

